# Handshake discrimination — raising the projection ceiling

STATUS: IN PROGRESS (stub committed early; container restarts have killed agents mid-flight).

## Task
The normalization-collision audit measured a hard ceiling: the 49 handshake cases
produce only 26 DISTINCT scored observations under the `handshake.judged` projection.
27 of 49 cases share an observation; the largest equivalence class holds 11.

This branch attempts to raise the ceiling by extending the projection to carry
distinctions the wire actually carries — NOT by adjusting a rule to make a
collision disappear.

## Findings
(to be filled in — nothing claimed yet)
