# Legacy record adjudication — WORK IN PROGRESS

Track: classify the 49 pre-vocabulary delta-ledger records under US-020 AC3
(`java-quirk` / `rust-defect` / `underspecified-behavior`) without rewriting the
frozen prefix and without breaking the handshake / public-corpus censuses.

Status: started. This stub exists so the branch is pushed before any real work,
per the container-restart discipline.
