# Rank 3 independence — work in progress

Branch: `claude/oracle-rank3-independence` off mainline `58f3aa4`.

Goal: make `internal/oraclerank` rank 3 (independent neutral expectations) genuinely
independent of the Java-mirroring reference model, or establish rigorously that it
cannot be and say what that costs.

STATUS: stub pushed to protect against container restart. No findings yet.
