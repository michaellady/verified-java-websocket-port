# Normalization collision audit — WORK IN PROGRESS

Branch: `claude/normalization-collision-audit`, based on mainline `2c63205`.

Goal: enumerate the observation model's normalization surface and find, by
construction, every place where two genuinely different wire behaviours map onto
the same normalized observation.

Status: stub commit, pushed early so a container restart cannot lose the work.
