Java-to-Rust port PRD pack, part 4 of 7 (the pack grew to 7 parts): full text of master US-008, the currently open independent-acceptance gate for the WebSocket lab, and US-009, the private skill-draft extraction story. Both are passes:false and carry the running status log in their Notes.

### US-008 — Independently accept the completed Java-WebSocket laboratory
**Priority:** 2 | **Passes:** false | **Depends on:** US-007 | **Labels:** laboratory-1, acceptance, external-gate | **Worker:** ["code-reviewer","codex-reviewer","reality-checker","security-scanner","performance-benchmarker"] | **Model hint:** 

As an independent reviewer, I want to audit the completed first laboratory against the master assurance case so that skill extraction uses verified evidence rather than implementation confidence.

**Acceptance criteria:**
- Every story and gate in the Java-WebSocket child PRD passes on its pinned commit before this audit starts; otherwise this story records the external block and makes no acceptance decision.
- The child canonicalizes the retained recovered Autobahn PASS receipt, proves all 247/247 selected client cases and 247/247 selected server cases passed without another rerun, closes every differential trace mismatch, links declared formal checks to shipped Rust, passes fuzz/runtime/concurrency/held-out lanes, and reconciles every expected case in the evidence manifest.
- The accepted child includes independently recomputed Java-versus-Rust formal coverage over a language-neutral semantic-obligation denominator, with exact shipped production-symbol mappings, honest per-side evidence strength, assumptions and bounds, paired comparability, production linkage, refinement coverage, counterexample sensitivity, and zero blocking gaps; line coverage and weighted aggregate scores cannot substitute for obligation-level acceptance.
- Project-owned Rust contains no unsafe code, all dependency unsafe belongs to an enumerated and reviewed trusted-computing-base inventory, and security review has zero unresolved critical or high findings. Candidate code cannot supply its own sbx launcher or turn a self-consistent receipt into authority; accepted Docker sbx receipts derive the exact runtime/template/policy/input/output/cleanup identities and complete externally imposed per-workload resource envelope from the protected supervisor, while the classifier remains outside the candidate sandbox.
- Eligible mutation score is 100% with zero MISSED mutants; every equivalent or unviable classification has independent explicit review and remains in the report.
- Pre-registered paired workloads show a material peak or steady-state memory improvement and Rust matches or outperforms Java within every declared CPU, throughput, startup, latency, allocation, and resource non-regression envelope; raw samples, confidence intervals, effect sizes, power analysis, environment checks, and an independent confirmation run remain in the evidence DAG.
- A production-shaped WebSocket shadow and canary rehearsal at the declared Sans-I/O boundary demonstrates side-effect control, behavioral comparison, capacity, automatic abort, Java fallback, state reconciliation where applicable, and rollback without promoting the laboratory beyond the evidence-supported readiness state.
- At least two fresh adversarial reviews, one using the code diff and one using only the assurance case plus executable artifact, agree that every published claim is no stronger than its evidence.
- The laboratory ledger identifies which lessons are WebSocket-specific, candidate-portable, disproven, or still unknown.

**E2E tests:**
- Given the release-candidate Java and Rust artifacts on the pinned platform matrix, when an independent actor reruns the complete evidence manifest from a clean checkout through the accepted hostile-workload isolation profile and recomputes formal coverage, then the resulting content-addressed artifacts and canonical coverage report reconcile with the accepted run, disconnected targets or incompatible bounds and assumptions are rejected, measured performance remains outside the microVM boundary, and no test, proof, mutation, benchmark, or platform case is skipped or silently retried.

**Files:** laboratories/java-websocket/acceptance.md, laboratories/java-websocket/evidence-manifest.json, laboratories/java-websocket/formal-coverage.md, port-ledger.jsonl

**Notes:** External gate reconciled 2026-08-28: the child project now records 27/27 stories passes:true with metadata completionScope STORY_EXECUTION_COMPLETE, but those completions use mixed inherited, borrowed, independent, and owner-relaxed scopes and do not satisfy this master's original strong acceptance criteria. The final child evaluator at 1b3da7976848e949cc10fd23aa6f56031a494529 reports PASS_OWNER_RELAXED_PUBLIC_PROJECTION_MECHANICS and INDEPENDENT_ACCEPTANCE_BLOCKED: the supplied checkout is NOT_VERIFIED against the declared subject, 0/26 child entries are strongly accepted, all 24 formal obligations remain BLOCKED, and 14 blockers plus seven nonclaims prohibit an independent ACCEPTED/PUBLISHED result. Codex supplemental current-code verification now adds a repaired sealed Java authority replay (231/231 methods and 326/326 invocations, byte-identical inventory), a fresh 74-scenario Java/Rust differential replay with 296 process receipts, zero current mismatch, 112 append-only ledger records (103 finding witnesses plus nine harness-artifact corrections), and 7/7 controls, plus current Rust debug and release workspace gates with 224 tests per profile, a refreshed 178,475-case property campaign plus 110 seeded fuzz cases, current exact TLA model checks, and separate full/targeted external Rust mutation receipts (938 full mutants at 96.09% raw score, then 9/9 targeted kills for adjudicated live gaps). New owner-executed Kani evidence at 949cc55 directly targets the shipped frame decoder and mask symbols: nine hostile-domain harnesses pass twice with an identical semantic projection and nine exact source mutations all produce counterexamples. Widening the mask offset from u16 to every usize value exposed a real public-API overflow, repaired and covered at usize::MAX before the proofs were retained. The Kani evidence is single-host Darwin/arm64, non-sbx, non-independent, and covers nine obligations rather than the complete 24-obligation composition denominator. All focused verifiers, the strict and Java-compatibility differential paths, and the full Go/Rust gates pass through 2d88f88. These are material confidence gains but remain owner-executed supplemental evidence and do not convert the final evaluator's strong acceptance counts. Earlier strong gaps also remain visible: US-023 retains 44 blocked original parity gates, US-025 has no accepted measured two-host resource envelope, and US-026 remains CUTOVER_BLOCKED without live traffic, measured rollback/soak, or executable Java fallback drill. No provenance-distinct custodian, human/protected replay, independent performance confirmation, production-shaped rehearsal, signing, external publication, production deployment, or Java-removal authority exists. Therefore master US-008 remains passes:false and makes no acceptance decision; its acceptance/evidence/formal-coverage/ledger files are intentionally absent rather than fabricated. OWNER_ATTESTED_NOT_INDEPENDENT; independent_review_claimed:false; production:false; publication:false. 2026-08-28 supplemental correction: Codex expanded current shipped-symbol Kani evidence at 0cf36a9 to 11 harnesses over frame decoding, masking, close-code classification, and strict UTF-8; two baseline runs have an identical semantic projection and 11/11 mutations are killed. The driver now retains diagnostics before parsing, accepts both observed Kani summary shapes, and self-validates schema plus semantic invariants before PASS. Current-head qualification at 7f31512 and strict/Java-compat differential evidence at 27437ff were refreshed and fully reverified; the pinned-Java Go suite and 224 current Rust tests pass. This remains owner-attested single-host evidence and does not change the 0/26 strong acceptance, 24-obligation composition, independence, performance, cutover, or publication blockers; US-008 remains false. 2026-08-28 denominator correction: a new self-validating supplemental projection retained at a9bddc7 maps the immutable 24-obligation catalog to the retained Kani receipt. It records 11/24 Rust production-symbol proof coverage, 9/24 obligation-specific mutation sensitivity (11 killed exact canaries; length-canonical-7 and mask-involution lack dedicated mutations), 0/24 Java formal bindings, 0/24 refinement, and 0/24 aggregate coverage. It binds projection basis cbdcf4c, proof subject 0cf36a9, the 62-binding current-source qualification at 0200cee, and exact catalog/receipt Git objects; its read-only verifier passes. The frozen US-023 projection remains unchanged. US-008 remains false. 2026-08-29 mutation-sensitivity closure: Codex added exact production-source mutations for inline seven-bit lengths and mask involution, retained a new owner-executed Kani receipt at 17e92c5 with 11 harnesses passing twice, 13/13 exact canaries producing counterexamples, zero survivors, and an unchanged semantic projection, then retained a self-validating 24-row projection through e66602d. Typed results are Rust proof 11/24, mutation sensitivity 11/24, Java formal binding 0/24, refinement 0/24, and aggregate 0/24. Historical 9/24 mutation coverage remains separately verifier-readable. The full pinned-Java Go matrix passes after the schema/evidence migration. US-008 remains false because the remaining 13 obligations, Java/refinement, independence, performance, cutover, and publication blockers are unchanged. 2026-08-29 emitted-frame proof expansion: Codex retained owner-executed Kani receipt 467a224 with 12 shipped-symbol harnesses passing twice, identical semantic projections, 14/14 exact source-mutation canaries producing counterexamples, zero survivors, and 14 distinct mutation-covered catalog obligations. The new FrameEncoder::encode harness proves emitted frame octets and masking for every payload byte at lengths 0..4, text/binary opcodes, both FIN states, both endpoint roles, and every four-byte client mask key; it discharged 882 checks with nine unreachable checks and cost about 307 seconds per focused pass under an explicit ten-minute ceiling. The verified supplemental projection is Rust proof 14/24, mutation sensitivity 14/24, Java formal binding 0/24, refinement 0/24, and aggregate 0/24. Historical 11/9, 11/11, and current 14/14 projections are verifier-readable side by side after repairing historical qualification replay. A canonical 74-scenario differential refresh at 499db8f produced 296 process receipts, 7/7 killed controls, 103 reproducers, and zero flakes, current mismatches, unresolved differences, or normalization collisions; the complete pinned-Java Go suite then passed. US-008 remains false because ten Rust/mutation obligations, all Java/refinement obligations, independence, two-host performance, production-shaped cutover, publication, and signing remain blocked. 2026-08-29 fragment-composition expansion: Codex retained owner-executed Kani receipt e624399 with 13 shipped-symbol harnesses passing twice, identical semantic projections, 16/16 exact source-mutation canaries producing counterexamples, zero survivors, and 16 distinct mutation-covered catalog obligations. The new binary-continuation harness follows the production FragmentAccumulator plan/prepare/feed/commit lifecycle for all initial and continuation payload bytes at exact lengths 0..2 and both final states; it discharged 1,543 checks with ten unreachable checks in about 47.1 seconds. Its initial unwind-eight assertion failure remains adverse evidence; only the full unwind-twelve rerun qualified. Separate final-delivery and binary-payload mutations prevent one harness from inflating two obligation credits. Historical 11/9, 11/11, 14/14, and current 16/16 projections remain verifier-readable. Current qualification at 1c5563d and the 74-scenario differential evidence at 1965637 were refreshed; format, Clippy with warnings denied, 224 debug tests, 224 release tests, and the complete pinned-Java Go matrix pass. The verified supplemental projection is Rust proof 16/24, mutation sensitivity 16/24, Java formal binding 0/24, refinement 0/24, and aggregate 0/24. US-008 remains false because eight Rust/mutation obligations, all Java/refinement obligations, independence, two-host performance, production-shaped cutover, publication, signing, and Java-removal authority remain blocked. 2026-08-29 control proof-ladder expansion: Codex retained owner-executed Kani receipt 2531f12 with 14 shipped-symbol harnesses passing twice, identical semantic projections, 17/17 exact source-mutation canaries producing counterexamples, zero survivors, and 17 distinct mutation-covered catalog obligations. Full ConnectionCore::step and direct consume_frames control compositions each crossed the declared ten-minute ceiling without a verdict and receive no proof credit. The qualified lower rung composes is_observed_control, ControlPayload::from_frame, should_automatically_pong, and encode_automatic_pong over every payload byte at exact lengths 0..4, Ping and Pong, both roles, both policies, and both decoded mask observations; it discharged 1,127 checks with eleven unreachable checks in about 14.1 seconds. An exact mutation swapping Ping/Pong automatic-response policy produced a counterexample. Decoder/event wiring, public step accounting, and handshake-to-open composition remain explicit nonclaims. Historical 11/9, 11/11, 14/14, 16/16, and current 17/17 projections remain verifier-readable. Current qualification at 3665360 and the 74-scenario differential evidence at 4e28051 were refreshed; format, Clippy with warnings denied, 224 debug tests, 224 release tests, and the complete pinned-Java Go matrix pass. The verified supplemental projection is Rust proof 17/24, mutation sensitivity 17/24, Java formal binding 0/24, refinement 0/24, and aggregate 0/24. US-008 remains false because seven Rust/mutation obligations, all Java/refinement obligations, independence, two-host performance, production-shaped cutover, publication, signing, and Java-removal authority remain blocked. 2026-08-29 close-terminal lifecycle expansion: Codex retained owner-executed Kani receipt a2b00ef with 15 shipped-symbol harnesses passing twice, identical semantic projections, 18/18 exact source-mutation canaries producing counterexamples, zero survivors, and 18 distinct mutation-covered catalog obligations. The new bounded CloseMachine state-machine harness covers both initiators, local-first flush before or after peer close, peer-first peer echo and owned local-half admission, retained-byte accounting, exact EOF classes, terminal completion only after both halves and local-write flush, and reset for every local and peer payload byte at exact lengths 0..2. Both retained runs discharged 914 checks with four unreachable checks; the exact completion-before-flush mutation failed three checks with a concrete counterexample. ConnectionCore decoder and output wiring remain explicit nonclaims. Historical 11/9, 11/11, 14/14, 16/16, 17/17, and current 18/18 projections remain verifier-readable. Current qualification at a720825, projection basis 656b263, and the 74-scenario differential evidence at 46e0843 were refreshed; format, Clippy with warnings denied, 224 debug tests, 224 release tests, and the complete pinned-Java Go matrix pass. The verified supplemental projection is Rust proof 18/24, mutation sensitivity 18/24, Java formal binding 0/24, refinement 0/24, and aggregate 0/24. US-008 remains false because six Rust/mutation obligations, all Java/refinement obligations, independence, two-host performance, production-shaped cutover, publication, signing, and Java-removal authority remain blocked. 2026-08-29 typed protocol-fault expansion: Codex retained owner-executed Kani receipt 30ee613 with 16 shipped-symbol harnesses passing twice, identical semantic projections, 19/19 exact source-mutation canaries producing counterexamples, zero survivors, and 19 distinct mutation-covered catalog obligations. The new independent RFC decision table covers all 65,536 two-byte frame prefixes under both endpoint roles, preserves exact typed reserved-bit, reserved-opcode, fragmented-control, oversized-control, and incorrect-masking failures plus incomplete/admitted decisions, and discharged 1,275 checks with four unreachable checks. Extended-length contents, payload semantics, ConnectionCore terminal wiring, Java proof, refinement, and independence remain explicit nonclaims. The verified supplemental projection is Rust proof 19/24, mutation sensitivity 19/24, Java formal binding 0/24, refinement 0/24, and aggregate 0/24; five Rust/mutation obligations remain blocked. A first 74-scenario differential refresh was correctly rejected after rebuilding the current release testee changed its digest; the corrected receipt at f76602c binds the rebuilt binary and records 296 fresh processes, 7/7 controls, 103 reproducers, and zero flakes, current mismatches, unresolved differences, or normalization collisions. Format, strict Clippy, 224 debug tests, 224 release tests, Go vet/build, and the complete pinned-Java Go matrix pass. US-008 remains false because Java/refinement coverage, independence, two-host performance, production-shaped cutover, publication, signing, and Java-removal authority remain blocked.

### US-009 — Extract the private port-java-to-rust draft skill
**Priority:** 2 | **Passes:** false | **Depends on:** US-006, US-008 | **Labels:** skill, private-draft, extraction | **Worker:** ["architect","knowledge-curator","code-reviewer","codex-reviewer"] | **Model hint:** 

As the program owner, I want a private draft extracted only from accepted WebSocket evidence so that CommonMark can test transfer instead of receiving a polished hindsight narrative.

**Acceptance criteria:**
- The draft uses the mike-skills structure and contains a concise portable SKILL.md, directly linked decision references, reusable evidence templates, and only minimal runtime adapters proven necessary by US-006. Its portable contract names isolation capabilities rather than one vendor; Docker sbx is the verified macOS profile, and any other runtime must prove semantically equivalent clone/source, secret, network, resource, output, and cleanup controls.
- Every normative instruction cites at least one accepted laboratory decision, failure, or evidence artifact; WebSocket-only steps are labeled as profiles or case-study material rather than universal rules.
- The skill begins with provenance/license, migration-mode, JavaIntakeManifest, versioned semantic-ID migration map, Port Seam Dossier, Behavior Delta Ledger, source-surface, oracle-strength, semantic-gap, verification-seam, CutoverContract, and assurance-claim classification before any translation step.
- The workflow enforces parity then refinement, vertical runnable slices, retained Java oracle, no stubs/deleted or weakened requirement-bearing tests/silent skips, independent review, pinned blocking suites plus latest canaries, claim-scoped formal evidence, first-party unsafe prohibition, pre-registered resource-performance confirmation, shadow or consumer replay, rollback rehearsal, and release soak.
- The developer-tool profile defaults to mandatory JDT LS for Java comprehension and rust-analyzer for Rust; it offers a pinned mutually exclusive Glancer experiment only when measured resource or agent-workflow benefits justify its limitations, and states that every LSP result is DeveloperToolRun non-assurance evidence.
- A skill validator and miniature fixture pass in both Claude Code and Codex; the draft remains private and version-pinned for the CommonMark forward test.
- A changelog records which WebSocket findings were deliberately not generalized.

**E2E tests:**
- Given a fresh Claude Code session and a fresh Codex session with only the private draft and miniature fixture, when each performs preflight, then both produce matching schema-valid decisions and refuse to start translation when a source pin, oracle policy, or evidence seam is missing.

**Files:** mike-skills/skills/port-java-to-rust/SKILL.md, mike-skills/skills/port-java-to-rust/references/, mike-skills/skills/port-java-to-rust/assets/, skill-revisions.jsonl

**Notes:** Skill-creator guidance was applied on 2026-08-28 to create the private experimental draft at companies/enterprise-vibe-code/skills/port-java-to-rust. The bundled quick validator passes in a disposable pinned-PyYAML environment. Initial dogfooding added the separate canonical-raw versus host-path-free portable-projection rule and caught an actual cross-runtime defect: HQ-only skill_uid/tags frontmatter was rejected by the Codex validator and removed in favor of the shared minimal schema. A second dogfooding correction added production-symbol Kani binding, hostile full-width symbolic domains, semantic repeat reconciliation, and one exact source mutation per claimed obligation after a friendly u16 domain missed a real usize overflow. A third correction records that generated-tool normalization of a bound manifest such as Cargo.lock must be compared semantically where formatting is not the contract, followed by explicit refresh of every downstream digest-bound receipt; stale provenance must fail closed and must not be dismissed as test noise. A contaminated, explicitly non-scoring CommonMark preflight produced a fourth correction: Surefire per-class XML collapsed parameterized spec classes to misleading zero-count summaries while Maven's real runner executed 2,253 tests, so the draft now requires a frozen semantic test inventory, runner-level denominator reconciliation, skip/filter accounting, and direct discovery canaries before assigning TESTED. The draft is now 0.1.4-private-draft; reintroduced HQ-only frontmatter was removed again and the shared Codex quick validator passes. It incorporates the dual Java-fidelity/RFC profiles, exact-binary freshness, explicit UTF-8 process protocol, four-process replay, append-only harness corrections, capability-based sbx isolation, claim-scoped assurance, bounded review, and parity-versus-cutover lessons. It remains passes:false because US-008 strong acceptance is still blocked and this full draft has not yet passed clean Claude Code and Codex miniature forward tests. Do not publish. Draft 0.1.5-private-draft adds W16 self-validating evidence producers after the expanded Kani lane exposed an optional output-shape parser bug, a stale nine-item schema, and a Rust-2021 versus Rust-2024 formatter mismatch. The draft now requires canonical formatting before expensive evidence, retained sanitized diagnostics before parsing, golden output-shape fixtures, producer-side schema and semantic verification before PASS, read-only post-retention verification, and dependency-ordered receipt refresh. The shared Codex validator passes after removing reintroduced HQ-only frontmatter. US-009 remains false because US-008 strong acceptance and clean fresh-session Claude Code/Codex forward tests are still blocked; do not publish. 2026-08-28 denominator correction: a new self-validating supplemental projection retained at a9bddc7 maps the immutable 24-obligation catalog to the retained Kani receipt. It records 11/24 Rust production-symbol proof coverage, 9/24 obligation-specific mutation sensitivity (11 killed exact canaries; length-canonical-7 and mask-involution lack dedicated mutations), 0/24 Java formal bindings, 0/24 refinement, and 0/24 aggregate coverage. It binds projection basis cbdcf4c, proof subject 0cf36a9, the 62-binding current-source qualification at 0200cee, and exact catalog/receipt Git objects; its read-only verifier passes. The frozen US-023 projection remains unchanged. US-008 remains false. Draft 0.1.6-private-draft adds W17 typed denominators after 11 killed canaries were found to cover only nine distinct obligations. The skill now requires separate harness, canary, mutation-covered-obligation, and catalog counts; ambiguous N/N labels are prohibited, and narrative/blocker counts must be generated from validated projections. Two new canaries bring current WebSocket mutation sensitivity to 11/24 without inflating aggregate coverage. The shared Codex validator passes after again removing HQ-only skill_uid/tags frontmatter. US-009 remains false pending US-008 strong acceptance and clean fresh-session Claude Code/Codex forward tests; do not publish. Draft 0.1.7-private-draft adds W18 costed symbolic domains and strengthens W15 evidence-DAG refresh. It records proof domain, checks, duration and timeout disposition; forbids silently shrinking a completed hostile domain merely to reduce runtime; and treats timeouts or decomposed composition as blockers. The current WebSocket dogfood advances Rust proof and mutation sensitivity to 14/24 while aggregate coverage remains 0/24. A current-head qualification refresh also demonstrated topological invalidation: the canonical differential manifest failed closed until a fresh 74-scenario replay, and historical projections were repaired to validate their exact era-bound qualification bytes rather than today's mutable alias. Reintroduced HQ-only skill_uid/tags frontmatter was removed and the shared Codex quick validator reports Skill is valid. US-009 remains false pending strong US-008 acceptance and clean fresh-session Claude Code/Codex forward tests; do not publish. Draft 0.1.8-private-draft adds W19 composed production seams. It prefers a real lifecycle chain over proof-only helpers, requires the exact phase chain and enclosing-path nonclaim, and preserves separate typed obligation accounting when one harness proves several properties. Each credited obligation still requires its own exact production-source mutation. Unwind and loop-bound failures are blocking harness-configuration failures until a declared bound and complete rerun close them. The current WebSocket dogfood advances Rust proof and mutation sensitivity to 16/24 while Java formal binding, refinement, and aggregate coverage remain 0/24. Reintroduced HQ-only skill_uid/tags frontmatter was removed again. US-009 remains false pending strong US-008 acceptance and clean fresh-session Claude Code/Codex forward tests; do not publish. Draft 0.1.9-private-draft adds W20 proof-composition ladders. It predeclares strongest public, internal-composition, and shipped-helper rungs with separate domains, ceilings, verdicts, and nonclaims. A live solver that reaches its ceiling without a verdict is failed evidence. A completed lower rung cannot inherit decoder, event, lifecycle, public-entry, or cross-runtime composition from a timed-out outer rung. The current WebSocket dogfood retains two no-verdict ten-minute control attempts and qualifies only the 1,127-check helper composition, advancing Rust proof and mutation sensitivity to 17/24 while Java formal binding, refinement, and aggregate coverage remain 0/24. Reintroduced HQ-only skill_uid/tags frontmatter was removed again. US-009 remains false pending strong US-008 acceptance and clean fresh-session Claude Code/Codex forward tests; do not publish. 2026-08-29 close-terminal dogfood advanced the verified WebSocket Rust proof and mutation projections to 18/24 while Java formal binding, refinement, and aggregate coverage remain 0/24. The retained CloseMachine lifecycle proof followed W19/W20's existing internal-composition rung and explicit outer-wiring nonclaim, so it introduced no new portable workflow rule and the skill deliberately remains 0.1.9-private-draft. The shared Codex validator remains green; US-009 remains false and publication remains prohibited. Draft 0.1.10-private-draft strengthens W04 exact-executable freshness after an apparently Kani-only source edit was initially compared with the prior release testee. A current rebuild changed the binary digest, the read-only verifier rejected the stale receipt, and the complete 74-scenario/296-process differential sweep was rerun against the rebuilt executable before receiving credit. The draft also records the independent two-byte protocol-fault decision-table dogfood that advanced current Rust proof and exact-mutation coverage to 19/24 without inflating Java, refinement, aggregate, independence, or publication claims. Reintroduced HQ-only skill_uid/tags frontmatter was removed and the shared Codex quick validator reports Skill is valid. US-009 remains false pending strong US-008 acceptance and clean fresh-session Claude Code/Codex forward tests; do not publish.
