//! Connection state-machine placeholder (UNIMPLEMENTED).
//!
//! Reserved for the deterministic `ConnectionCore` state machine and its
//! contract types -- immutable configuration with checked limits, role,
//! transport-byte input, local commands, ordered transport writes, semantic
//! events, connection state, and typed protocol failures (US-009), plus the
//! single-owner bounded command boundary (US-017). This module intentionally
//! exports nothing: no state machine, configuration type, event type, or
//! error type exists yet, and none of the US-009 acceptance criteria are
//! claimed by this scaffold.
